# BIND DNS Manager - Environment Configuration
# 
# This file contains all the configuration needed to connect to your BIND DNS server
# Copy this file to .env and fill in your actual values

# BIND Server Connection (Required)
# The hostname or IP address of your BIND DNS server
BIND_HOST=dns.example.com

# SSH port for connecting to the BIND server (default: 22)
BIND_PORT=22

# SSH username with access to BIND zone files and rndc
BIND_USER=root

# Authentication (Choose ONE method: SSH Key OR Password)
# Path to SSH private key file (e.g., /root/.ssh/id_rsa or ~/.ssh/id_rsa)
BIND_SSH_KEY=/root/.ssh/id_rsa

# OR use password authentication (if not using SSH key)
# BIND_PASSWORD=your-ssh-password

# DNS Zone Configuration (Required)
# The DNS zone/domain you want to manage
DNS_ZONE=example.com

# Full path to the BIND zone file on the server
# Common locations:
#   Debian/Ubuntu: /etc/bind/zones/db.example.com
#   RHEL/CentOS: /var/named/example.com.zone
ZONE_FILE_PATH=/etc/bind/zones/db.example.com

# Flask Configuration (Optional)
# FLASK_ENV=production
# FLASK_DEBUG=False
